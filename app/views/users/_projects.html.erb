<ul class="list-unstyled text-start text-white">
    <% user.projects.each do |project| %>
        <li class='pt-2'><strong class='text-muted'><%= project.phase.name %>:</strong> <%= link_to project.name, project_path(project), class: "text-decoration-none text-white" %></li>
    <% end %>
</ul>