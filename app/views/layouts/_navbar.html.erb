<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to "Flatiron Project Mode", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item"><%= link_to "Add Project", new_project_path, class: "nav-link" %></li>
        <li class="nav-item"><%= link_to "My Projects", user_path(current_user), class: "nav-link" %></li>
        <li class="dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
            Modules
          </a>
          <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
            <% Phase.all.each do |phase|  %>
              <li class="nav-link"><%= nav_link(phase) %></li>
            <% end  %>
          </ul>
        </li>
        <li class="nav-item"><%= link_to "Log Out", logout_path, method: 'delete', class: "nav-link" %></li>
        <%= link_to(user_path(current_user), class: %w(nav-link position-absolute top-50 end-0 translate-middle)) do %>
            <%= display_avatar(current_user) %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>


